#!/usr/bin/env python3
"""Cline TaskCancel hook — retain the partial transcript of a cancelled task."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from lib.hooks_impl import main_task_cancel

main_task_cancel()
