#!/usr/bin/env python3
"""Cline TaskStart hook — recall context for the new task and seed the transcript."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from lib.hooks_impl import main_task_start

main_task_start()
